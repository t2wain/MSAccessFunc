namespace MSAccessLib
{
    /// <summary>
    /// An application marker interface to 
    /// distinguish from the standard interface
    /// </summary>
    public interface IDisposable2
    {
        void Dispose();
    }
}
